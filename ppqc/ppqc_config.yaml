# Autogenerated by SRA conversion

name: ppqc

metadata:
  sample_annotation: ppqc_annotation_revised.csv
  sample_subannotation: ppqc_subannotation.csv
  output_dir: ${PROCESSED}/ppqc
  pipeline_interfaces: ${CODE}/peppro/pipeline_interface.yaml

derived_columns: [read1, read2]

data_sources:
  SRA: "${SRABAM}/{SRR}.bam"
  MJGR1: "$DATA/guertin/proseq/fastq/{sample_name}_PE1.fastq.gz"
  MJGR2: "$DATA/guertin/proseq/fastq/{sample_name}_PE2.fastq.gz"
  

implied_columns:
  organism:
    "Mus musculus":
      genome: mm10
    "Homo sapiens":
      genome: hg38
    "Rattus norvegicus":
      genome: rn6
    "Drosophila melanogaster":
      genome: dm3

subprojects:
  sra_convert:
    metadata:
      pipeline_interfaces: ${CODE}/sra_convert/pipeline_interface_convert.yaml
    data_sources:
     SRA: "${SRARAW}/{SRR}.sra"

